class Repository {
    constructor(user_id, repo_title, repo_add, repo_readMe) {
        this.user_id = user_id;
        this.repo_title = repo_title;
        this.repo_add = repo_add;
        this.repo_readMe = repo_readMe;
    }
}
